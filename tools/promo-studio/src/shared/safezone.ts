/**
 * 쇼츠/릴스 세로 영상(1080×1920, 9:16)의 안전 여백.
 * 기기마다 화면 비율이 조금씩 달라(9:19.5, 9:20 등) 위아래가 살짝 잘리거나,
 * 인스타그램 자체 UI(캡션 텍스트·아이디·음악 정보·버튼)가 화면 아래쪽을 가릴 수 있다.
 * 이 범위 밖에는 자막처럼 반드시 보여야 하는 요소를 두지 않는다.
 */
export const SAFE_TOP = 200
export const SAFE_BOTTOM = 320 // 화면 맨 아래로부터의 거리
export const SAFE_BOTTOM_Y = 1920 - SAFE_BOTTOM // = 1600, 안전 영역의 아래쪽 경계선(y좌표)
