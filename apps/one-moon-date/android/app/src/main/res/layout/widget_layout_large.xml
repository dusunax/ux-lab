<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:background="@drawable/widget_background"
    android:clipToOutline="true">

    <!-- 월 표시 (빨간 헤더) -->
    <TextView
        android:id="@+id/widget_month"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/widget_month_header"
        android:gravity="center"
        android:paddingTop="12dp"
        android:paddingBottom="10dp"
        android:text="1월"
        android:textColor="@color/white"
        android:textSize="16sp"
        android:textStyle="bold"
        android:letterSpacing="0.1" />

    <!-- 일 표시 (큰 숫자). 실제 폰트 크기는 LunarWidgetLargeProvider가 위젯 실제 크기에 맞춰 계산한다.
         1x1 미리보기 박스의 day 영역(세로 124px) 대비 안전한 잉크 비율(24sp)을 실측하고, 2x2
         미리보기 박스의 day 영역(세로 319px, 1x1의 약 2.57배)에 같은 비율로 맞춰 60sp로 산정 —
         "8"로 실기기 재확인 완료. 처음 선제적으로 반토막 낸 40sp는 박스 대비 너무 작아 보였다. -->
    <TextView
        android:id="@+id/widget_day"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:paddingBottom="3dp"
        android:gravity="center_horizontal|center_vertical"
        android:textAlignment="center"
        android:text="1"
        android:textColor="@color/text_primary_light"
        android:textSize="60sp"
        android:textStyle="bold"
        android:fontFamily="sans-serif-light" />

</LinearLayout>
