# Sprint 3 회의록

**날짜:** 2026-05-18  
**참석자:** Jordan (PM), Riley (UX), Avery (FE), Blake (BE), Chase (SRE), Morgan (QA), Alex (TS)  
**진행자:** Jordan  

---

## 안건

1. ai-empathy-diary 서비스 품질 다듬기
2. 웹 배포: Vercel
3. Auth 구현: Firebase Google 로그인 OAuth
4. DB 구성: Firestore
5. 사용자 로그 수집 환경 구성

---

## 논의 내용

### 1. ai-empathy-diary 서비스 품질 다듬기

Sprint 2까지 완성된 기능(감정 필터, 삭제 버튼, 빈 셀 그리드 배경)을 기반으로 실제 서비스 수준으로 다듬는다.  
UX 개선, 에러 처리, 빈 상태(empty state) 대응 등이 포함된다.

### 2. 웹 배포: Vercel

- 배포 대상: ai-empathy-diary
- 플랫폼: Vercel (MCP 설치 완료)
- 환경 변수 관리 및 프리뷰 배포 전략 포함

### 3. Auth 구현: Firebase Google 로그인 OAuth

- Firebase Authentication 사용 (Firebase MCP 설치 완료)
- Google OAuth Provider — `signInWithPopup(new GoogleAuthProvider())`
- 로그인/로그아웃 상태에 따른 UI 분기 처리

### 4. DB 구성: Firestore

- 사용자별 일기 데이터 저장 구조 설계
- Firebase Firestore 사용
- 보안 규칙(Security Rules) 설정 — 본인 데이터만 읽기/쓰기

### 5. 사용자 로그 수집 환경 구성

- 어떤 행동을 기록할지 정의 필요
- Firebase Analytics 또는 별도 로그 수집 방식 검토
- Chase가 인프라 방향 제안 예정

---

## 결정 사항

| 번호 | 결정 내용 | 담당 |
|------|-----------|------|
| 1 | Auth는 Firebase Google OAuth로 확정 | Avery + Blake |
| 2 | DB는 Firestore, 컬렉션 구조는 Blake가 설계 | Blake |
| 3 | 배포는 Vercel, 환경 변수 세팅 포함 | Chase |
| 4 | 로그 수집 방식은 Chase가 다음 회의 전까지 안 제안 | Chase |

## 액션 아이템

- [x] Vercel 배포 및 도메인 연결 (Chase)
- [x] Firebase 프로젝트 생성 및 Google OAuth 설정 (Blake)
- [x] Google 로그인 UI 및 Auth 상태 연동 (Avery)
- [x] Firestore 컬렉션 구조 설계 및 보안 규칙 작성 (Blake)
- [x] 로그 수집 환경 방안 제안 (Chase)
- [x] 서비스 품질 점검 및 UX 개선 항목 정리 (Riley)

---

## 비고

- 팀명: 수산시장
- MCP 스택: Vercel, Firebase, Supabase, GitHub, Context7, Playwright 설치 완료
- 다음 회의에서 로그 수집 방안 및 Firestore 스키마 확정 예정

---

## 로그 수집 환경 방안 (Chase 제안)

**제안일:** 2026-05-18  
**제안자:** Chase (SRE)

### 수집 면 (2개)

| 수집 위치 | 도구 |
|---|---|
| 클라이언트 (UX 이벤트) | Firebase Analytics |
| 서버 (신뢰성 이벤트) | Vercel Runtime Logs (structured JSON) |

Firestore 로그 컬렉션은 기각 — 쓰기 비용, 쿼리 UX 부재, 이미 GA/Vercel이 무료로 제공.

### 수집 이벤트

**클라이언트 (Firebase Analytics)**

| 이벤트 | 주요 파라미터 | 목적 |
|---|---|---|
| `auth_signin_attempt` | `method=google` | OAuth 마찰 감지 |
| `auth_signin_success` | `method`, `is_new_user` | 전환율 |
| `auth_signin_failure` | `code` | 인증 오류 추적 |
| `auth_signout` | — | 세션 길이 추정 |
| `entry_submit_attempt` | `text_length` | 퍼널 진입 |
| `entry_submit_success` | `text_length`, `emotion`, `latency_ms` | 퍼널 완료 + 성능 |
| `entry_submit_failure` | `reason` | 실패 원인 분류 |
| `entry_delete` | — | 참여도 |
| `entry_load_failure` | `code` | Firestore 읽기 오류 |
| `entry_save_failure` | `code` | Firestore 쓰기 오류 |

**서버 (Vercel Runtime Logs)**

| 이벤트 | 주요 필드 |
|---|---|
| `chat_request` | `request_id`, `model_requested`, `is_auto` |
| `chat_fallback` | `request_id`, `from_model`, `to_model`, `attempt` |
| `chat_upstream_error` | `request_id`, `model`, `status`, `latency_ms` |
| `chat_success` | `request_id`, `model_used`, `attempt_count`, `latency_ms` |
| `chat_exhausted` | `request_id`, `attempts` |

`x-request-id` 헤더로 클라이언트-서버 이벤트 연결.

### 절대 수집 금지 (개인정보)

- 일기 텍스트 (`entry.text`) — 길이(`text_length`)만 기록
- AI 공감 응답 (`entry.empathy`)
- 이메일 · 이름 · UID 원문
- OpenRouter 요청/응답 본문 — 토큰 수만 기록
- Firebase 오류 `message` — `code`만 기록

### 구현 우선순위

| 순위 | 항목 | 예상 공수 |
|---|---|---|
| P0 (이번 스프린트) | `chat_fallback/exhausted/success` (서버) | ~30줄 |
| P0 (이번 스프린트) | `entry_submit_success/failure` (클라이언트) | ~6줄 |
| P0 (이번 스프린트) | `auth_signin_success/failure` (클라이언트) | ~4줄 |
| P1 (다음 스프린트) | `x-request-id` 연결, Firestore 오류 이벤트 | — |
| P2 (DAU 50+ 이후) | `entry_delete`, `sheet_open`, 감정 분포 | — |

### 다음 회의 논의 사항

- 동의 UX 배너: 지금 추가 vs 해외 출시 시점
- `x-request-id`: 응답 헤더 vs JSON body 포함
- GA4 데이터 보존 기간 (Chase 제안: 2개월)

---

## P0 로그 구현 완료

**완료일:** 2026-05-18  
**구현:** Chase 제안 기반, Blake(BE) + Morgan(QA) 검증 후 수정

### 구현 내용

**서버 (`api/chat.js`)**
- `log()` 헬퍼 — structured JSON (`console.log`) via Vercel Runtime Logs
- `x-request-id` 헤더 — 클라이언트-서버 이벤트 연결용
- 이벤트: `chat_request`, `chat_fallback`, `chat_success`, `chat_upstream_error`, `chat_exhausted`
- 5xx 응답도 429와 동일하게 다음 모델로 fallback 재시도

**서버 (`api/log.js`)** — 신규 생성
- 클라이언트 이벤트 릴레이 엔드포인트 (Firebase Analytics 대신 Vercel Runtime Logs 통합)
- 이벤트 allowlist 9개 + 파라미터 키 allowlist로 로그 인젝션 방지
- `sendBeacon` 기반 논블로킹 전송

**클라이언트 (`index.html`)**
- `logEvent(event, params)` — `sendBeacon` 래퍼, try/catch로 UX 영향 없음
- `errorReason(err)` — 에러 메시지 → reason 코드 매핑
- 수집 이벤트: `auth_signin_success`, `auth_signin_failure`, `entry_submit_attempt`, `entry_submit_success`, `entry_submit_failure`

### QA 리뷰 중 발견 및 수정된 사항

| 항목 | 발견자 | 내용 | 조치 |
|---|---|---|---|
| 파라미터 키 인젝션 | Morgan(QA), Blake(BE) | `event`/`ts` 필드를 params로 덮어쓸 수 있었음 | 키 allowlist 추가, 예약 필드를 spread 뒤에 배치 |
| `crypto.randomUUID()` 글로벌 미보장 | Morgan(QA) | Node 18 일부 버전에서 미지원 | `import { randomUUID } from 'crypto'`로 교체 |
| `errorReason()` 누락 케이스 | Morgan(QA) | 3개 throw 경로가 `'unknown'` 반환 | `input_too_long`, `http_error`, `parse_error` 케이스 추가 |
| 5xx fallback 미처리 | Morgan(QA) | 5xx 응답 시 fallback 없이 즉시 반환 | 429와 동일하게 다음 모델로 재시도 처리 |

### 미해결 항목 (다음 스프린트)

- ⚠️ Open — **Rate Limiting**: `/api/log` 공개 엔드포인트에 Vercel Firewall IP 기반 rate limit 규칙 추가 필요
- ⚠️ Open — **Firebase Analytics 연결**: `measurementId` 미설정 상태 — Firebase Console에서 Google Analytics 연결 후 추가 예정
- ⚠️ Open — **P1 이벤트**: `entry_load_failure`, `entry_save_failure`, `entry_delete` call site 구현 (allowlist에는 등록됨)
